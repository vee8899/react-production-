// Storage bucket helpers. Add bucket-specific operations as needed.
// Example:
// export const uploadFile = (bucket: string, path: string, file: File) =>
//   supabase.storage.from(bucket).upload(path, file);

export const storage = {
  // Placeholder — add storage helpers here when needed
} as const;