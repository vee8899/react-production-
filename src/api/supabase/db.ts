// Typed DB query helpers. Replace with actual table queries once schema is known.
// Example:
// export const getUsers = () => supabase.from("users").select("*");

export const db = {
  // Placeholder — add typed queries here after generating Supabase types
} as const;