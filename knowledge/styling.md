# Styling

> Generated by `npm run ingest`. Source of truth remains the referenced implementation.

Global styles live in `src/index.css` and `src/styles/globals.css`; design tokens live in `src/styles/tokens.ts`. Tailwind CSS is configured through Vite. Motion primitives live under `src/components/motion/`.
