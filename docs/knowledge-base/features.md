# Features

> Generated by `npm run ingest`. Source of truth remains the referenced implementation.

## Public experience

Landing page, integrations orbit, ROI calculator, FAQ, contact form, and login entry point.

## Authenticated experience

Dashboard exposes client automation metrics and workflow activity. Workflows exposes configured automation services and runs.

## Automation contract

n8n sends `event_id`, `client_id`, `feature_type`, `workflow_name`, and `status` to `supabase/functions/ingest-run/index.ts`; the database migration enforces idempotency and enum constraints.
