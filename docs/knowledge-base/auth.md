# Authentication

> Generated by `npm run ingest`. Source of truth remains the referenced implementation.

`src/main.tsx` bootstraps Supabase auth and writes session state to `src/store/authStore.ts`. `useAuth` exposes the session to pages. `ProtectedRoute` redirects unauthenticated users to `/login`. Invite acceptance is handled at `/accept-invite`.
